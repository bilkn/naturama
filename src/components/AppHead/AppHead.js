import React from 'react';
import Logo from '../Logo/Logo';
import './AppHead.scss';

function AppHead() {
  return (
    <header className="app-head">
      <Logo />
    </header>
  );
}

export default AppHead;
